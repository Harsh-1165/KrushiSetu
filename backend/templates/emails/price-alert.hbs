<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Price Alert</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; line-height: 1.6; background-color: #f4f7f6; }
    .container { max-width: 600px; margin: 0 auto; background: #ffffff; }
    .header { background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%); padding: 30px; text-align: center; }
    .header h1 { color: #ffffff; font-size: 22px; }
    .content { padding: 30px; }
    .alert-badge { display: inline-block; background: #fef3c7; color: #92400e; padding: 6px 16px; border-radius: 20px; font-size: 13px; font-weight: 500; margin-bottom: 20px; }
    .price-card { background: linear-gradient(135deg, #fef3c7 0%, #fde68a 100%); padding: 30px; border-radius: 12px; text-align: center; margin-bottom: 25px; }
    .crop-name { font-size: 24px; color: #78350f; font-weight: 700; }
    .price-display { font-size: 48px; color: #92400e; font-weight: 700; margin: 15px 0; }
    .price-display small { font-size: 18px; font-weight: 400; }
    .price-change { font-size: 16px; padding: 8px 16px; border-radius: 20px; display: inline-block; }
    .price-change.up { background: #dcfce7; color: #166534; }
    .price-change.down { background: #fef2f2; color: #991b1b; }
    .mandi-info { text-align: center; color: #78350f; font-size: 14px; margin-top: 15px; }
    .details-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 15px; margin: 25px 0; }
    .detail-box { background: #f9fafb; padding: 15px; border-radius: 8px; text-align: center; }
    .detail-box label { display: block; color: #6b7280; font-size: 12px; margin-bottom: 5px; }
    .detail-box value { display: block; color: #1f2937; font-size: 16px; font-weight: 600; }
    .cta-button { display: block; background: #f59e0b; color: #ffffff !important; text-decoration: none; padding: 14px; border-radius: 8px; font-weight: 600; text-align: center; }
    .secondary-link { display: block; text-align: center; margin-top: 15px; color: #6b7280; font-size: 14px; }
    .secondary-link a { color: #f59e0b; text-decoration: none; }
    .footer { background: #f9fafb; padding: 25px; text-align: center; }
    .footer p { color: #6b7280; font-size: 13px; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>📈 Price Alert Triggered!</h1>
    </div>
    
    <div class="content">
      <p style="margin-bottom: 15px;">Hi {{userName}},</p>
      
      <span class="alert-badge">Alert: {{condition}} ₹{{targetPrice}}</span>
      
      <div class="price-card">
        <p class="crop-name">{{cropName}}</p>
        <p class="price-display">₹{{currentPrice}} <small>/quintal</small></p>
        <span class="price-change {{trend}}">
          {{#ifEquals trend "up"}}↑{{/ifEquals}}
          {{#ifEquals trend "down"}}↓{{/ifEquals}}
          {{priceChangePercent}}% (₹{{priceChange}})
        </span>
        <p class="mandi-info">at {{mandiName}}</p>
      </div>
      
      <div class="details-grid">
        <div class="detail-box">
          <label>Your Target</label>
          <value>₹{{targetPrice}}</value>
        </div>
        <div class="detail-box">
          <label>Condition</label>
          <value>{{condition}}</value>
        </div>
      </div>
      
      <a href="{{mandiUrl}}" class="cta-button">View Market Details →</a>
      
      <p class="secondary-link">
        <a href="{{alertsUrl}}">Manage your price alerts</a>
      </p>
    </div>
    
    <div class="footer">
      <p>This is an automated alert based on your preferences.</p>
      <p style="margin-top: 10px;">© {{year}} GreenTrace</p>
    </div>
  </div>
</body>
</html>
