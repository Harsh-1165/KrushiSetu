<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Order Confirmation</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; line-height: 1.6; background-color: #f4f7f6; }
    .container { max-width: 600px; margin: 0 auto; background: #ffffff; }
    .header { background: linear-gradient(135deg, #16a34a 0%, #15803d 100%); padding: 30px; text-align: center; }
    .header h1 { color: #ffffff; font-size: 24px; margin-bottom: 5px; }
    .order-number { color: rgba(255,255,255,0.9); font-size: 14px; }
    .success-badge { display: inline-block; background: rgba(255,255,255,0.2); color: white; padding: 8px 20px; border-radius: 20px; margin-top: 15px; font-size: 14px; }
    .content { padding: 30px; }
    .section { margin-bottom: 25px; }
    .section-title { font-size: 16px; color: #1f2937; font-weight: 600; margin-bottom: 15px; padding-bottom: 10px; border-bottom: 2px solid #e5e7eb; }
    .item { display: flex; padding: 15px 0; border-bottom: 1px solid #f3f4f6; }
    .item-image { width: 60px; height: 60px; background: #f3f4f6; border-radius: 8px; margin-right: 15px; }
    .item-details { flex: 1; }
    .item-name { font-weight: 500; color: #1f2937; font-size: 14px; }
    .item-meta { color: #6b7280; font-size: 13px; margin-top: 4px; }
    .item-price { font-weight: 600; color: #1f2937; text-align: right; }
    .summary-row { display: flex; justify-content: space-between; padding: 8px 0; font-size: 14px; }
    .summary-row.total { border-top: 2px solid #1f2937; margin-top: 10px; padding-top: 15px; font-weight: 700; font-size: 16px; }
    .address-box { background: #f9fafb; padding: 20px; border-radius: 8px; }
    .address-box h4 { font-size: 14px; color: #6b7280; margin-bottom: 10px; }
    .address-box p { color: #1f2937; font-size: 14px; }
    .delivery-info { background: #ecfdf5; padding: 20px; border-radius: 8px; margin-top: 20px; }
    .delivery-info h4 { color: #059669; font-size: 14px; margin-bottom: 8px; }
    .delivery-info p { color: #065f46; font-size: 14px; }
    .cta-button { display: block; background: #16a34a; color: #ffffff !important; text-decoration: none; padding: 14px; border-radius: 8px; font-weight: 600; text-align: center; margin-top: 25px; }
    .footer { background: #f9fafb; padding: 25px; text-align: center; }
    .footer p { color: #6b7280; font-size: 13px; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>Order Confirmed!</h1>
      <p class="order-number">Order #{{orderNumber}}</p>
      <span class="success-badge">✓ Payment Successful</span>
    </div>
    
    <div class="content">
      <p style="color: #4b5563; margin-bottom: 25px;">
        Hi {{userName}}, thank you for your order! We've received your order and will begin processing it soon.
      </p>
      
      <!-- Order Items -->
      <div class="section">
        <h3 class="section-title">Order Items</h3>
        {{#each items}}
        <div class="item">
          <div class="item-image"></div>
          <div class="item-details">
            <p class="item-name">{{this.name}}</p>
            <p class="item-meta">Qty: {{this.quantity}} {{this.unit}} • Seller: {{this.sellerName}}</p>
          </div>
          <div class="item-price">{{formatCurrency this.total}}</div>
        </div>
        {{/each}}
      </div>
      
      <!-- Order Summary -->
      <div class="section">
        <h3 class="section-title">Order Summary</h3>
        <div class="summary-row">
          <span>Subtotal</span>
          <span>{{formatCurrency subtotal}}</span>
        </div>
        <div class="summary-row">
          <span>Shipping</span>
          <span>{{formatCurrency shipping}}</span>
        </div>
        <div class="summary-row">
          <span>Tax (GST)</span>
          <span>{{formatCurrency tax}}</span>
        </div>
        <div class="summary-row total">
          <span>Total</span>
          <span>{{formatCurrency total}}</span>
        </div>
      </div>
      
      <!-- Shipping Address -->
      <div class="section">
        <h3 class="section-title">Shipping Address</h3>
        <div class="address-box">
          <p><strong>{{shippingAddress.fullName}}</strong></p>
          <p>{{shippingAddress.addressLine1}}</p>
          {{#if shippingAddress.addressLine2}}
          <p>{{shippingAddress.addressLine2}}</p>
          {{/if}}
          <p>{{shippingAddress.city}}, {{shippingAddress.state}} {{shippingAddress.pincode}}</p>
          <p>Phone: {{shippingAddress.phone}}</p>
        </div>
        
        <div class="delivery-info">
          <h4>Estimated Delivery</h4>
          <p>{{formatDate estimatedDelivery}}</p>
        </div>
      </div>
      
      <a href="{{trackingUrl}}" class="cta-button">Track Your Order →</a>
    </div>
    
    <div class="footer">
      <p>Questions about your order? Contact us at {{supportEmail}}</p>
      <p style="margin-top: 10px;">© {{year}} GreenTrace. All rights reserved.</p>
    </div>
  </div>
</body>
</html>
